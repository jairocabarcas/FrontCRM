import { FiltroContactosPipe } from './filtro-contactos.pipe';

describe('FiltroContactosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroContactosPipe();
    expect(pipe).toBeTruthy();
  });
});
